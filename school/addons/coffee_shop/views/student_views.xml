<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="student_form_view" model="ir.ui.view">
        <field name="name">student.form.view</field>
        <field name="model">student</field>
        <field name="arch" type="xml">
            <form string="Student">
                <sheet>
                    <div name="button_box" class="oe_button_box">
                        <button name="action_view_consumptions" type="object" icon="fa-shopping-cart" class="oe_stat_button">
                            <field string="Consumptions" name="count_consumptions" widget="statinfo"/>
                        </button>
                    </div>
                    <group>
                        <group>
                            <field name="sequence"/>
                            <field name="name"/>
                            <field name="middle_name"/>
                            <field name="last_name"/>
                            <field name="carrer_id" options="{'no_create': True, 'no_create_edit': True}"/>
                            <field name="birth_date"/>
                            <field name="age" readonly="1"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="student_tree_view" model="ir.ui.view">
        <field name="name">student.tree.view</field>
        <field name="model">student</field>
        <field name="arch" type="xml">
            <tree string="Student Tree">
                <field name="name"/>
                <field name="middle_name"/>
                <field name="last_name"/>
            </tree>
        </field>
    </record>

    <record id="student_action" model="ir.actions.act_window">
        <field name="name">Students</field>
        <field name="res_model">student</field>
        <field name="view_mode">tree,form</field>
    </record>

    <menuitem id="menu_student_student" name="Students" action="student_action" parent="coffee_shop_general"/>
</odoo>